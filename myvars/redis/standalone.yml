# 源代码下载地址
source_code_download_url: "https://download.redis.io/redis-stable.tar.gz"

# 服务名称
service_name: "redis"

# 安装目录
install_dir: "/usr/local/redis"

# 数据目录
data_dir: "/data/redis"

# 主配置目录
config_dir: "/etc/redis"

# 登录密码
redis_password: "root"

# 日志文件目录
log_dir: "/var/log/redis"

# 日志文件名
log_file: "redis.log"

# pid文件目录
pid_dir: "/var/run/redis"

# pid文件目录
pid_file: "redis.pid"

# port
listen_port: 6379
